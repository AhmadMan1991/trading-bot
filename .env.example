# Copy this file to .env and fill in real values, then run locally with:
#   python3 main.py
# .env is already in .gitignore — it will never get committed.

TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=
TWELVEDATA_KEY=
OLLAMA_API_KEY=

# Optional — the macro step (step 2) skips itself cleanly if this is blank.
GEMINI_API_KEY=
